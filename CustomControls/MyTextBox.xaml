<UserControl x:Class="LoginForm.CustomControls.MyTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoginForm.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="myTextBox">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis"/>
    </UserControl.Resources>
    <StackPanel Margin="6 4">
        <!--<TextBlock Text="{Binding ElementName=myTextbox,
         Path=Caption}" 
                   Foreground="#97a5b4"
                   FontSize="13" 
                   Margin="0 0 0 8"/>-->
        <Grid>
            <TextBlock Margin="13 0" VerticalAlignment="Center" Foreground="#868686"
                       IsHitTestVisible="False" Panel.ZIndex="1" FontSize="14" 
                       Text="{Binding ElementName=myTextBox, Path=Hint}"
                       Visibility="{Binding ElementName=textBox, Path=Text.IsEmpty, Converter={StaticResource boolToVis}}"
                       />
            <TextBox x:Name="textBox"/>
        
        </Grid>
    </StackPanel>
</UserControl>
